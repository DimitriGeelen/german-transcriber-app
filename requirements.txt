fastapi
uvicorn[standard]
# openai-whisper # Whisper-timestamped includes this
python-dotenv # For loading .env files
python-multipart # For FastAPI file uploads
speechbrain
librosa # Added for audio loading/chunking
scikit-learn # For SpectralClustering / AgglomerativeClustering
soundfile # For saving resampled audio
whisper-timestamped # Added for accurate word timestamps
# torch # Remove generic torch

# Add PyTorch with CUDA 12.1 support (Check https://pytorch.org/ for specific versions if needed)
# Make sure CUDA Toolkit 12.1 is installed on the system
torch --index-url https://download.pytorch.org/whl/cu121
torchaudio --index-url https://download.pytorch.org/whl/cu121
# torchvision --index-url https://download.pytorch.org/whl/cu121 # Likely not needed

# Testing
pytest
pytest-asyncio # To run async tests
httpx # For async testing FastAPI endpoints 